<Window x:Class="MDF_Manager.SaveLibraryNotice"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MDF_Manager"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Save Library?" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label VerticalAlignment="Center" HorizontalAlignment="Center" Content="Save changes to the Library?" FontSize="20"/>
        <Rectangle Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Fill="#EEEEEE"/>
        <Button Grid.Row="1" Height="25" Width="100" VerticalAlignment="Center" HorizontalAlignment="Left" Content="Yes" Click="Confirm" Margin="50,0,0,0"/>
        <Button Grid.Row="1" Height="25" Width="100" VerticalAlignment="Center" HorizontalAlignment="Right" Content="No" Click="Deny" Margin="0,0,50,0"/>
    </Grid>
</Window>
